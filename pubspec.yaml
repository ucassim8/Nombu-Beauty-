name: nombu_beauty
description: "A new Flutter project for NOMBU Beauty services."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.3.0  # <--- Add this line here
  
  # The specific versions you sent
  image_picker: ^1.1.2
  url_launcher: ^6.3.1
  firebase_core: ^2.32.0
  cloud_firestore: ^4.17.5
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Your specific logo file name
  assets:
    - assets/Logonombu.jpg
